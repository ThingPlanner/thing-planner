package com.thingplanner;

import com.thingplanner.features.calendar.events.usecase.AddEventTest;
import io.quarkus.test.junit.QuarkusIntegrationTest;

@QuarkusIntegrationTest
class AddEventIT extends AddEventTest {
    // Execute the same tests but in packaged mode.
}
